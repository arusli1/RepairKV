# Initial Paper Draft

This folder contains an anonymous ICML-style initial draft written from
`../outline.md`.

Build from this directory with:

```bash
latexmk -pdf -interaction=nonstopmode -halt-on-error -outdir=../aux/initial_draft main.tex
```

The draft uses `../icml2026.sty` and existing figure PDFs from
`../figures/`.
